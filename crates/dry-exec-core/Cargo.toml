[package]
name = "dry-exec-core"
version = "0.1.0"
edition = "2021"
authors = ["dry-exec systems engineering"]
description = "Deterministic ephemeral execution primitive and state delta engine"

[dependencies]
libc = "0.2"
nix = { version = "0.29", features = ["process", "sched", "signal", "mman", "fs", "user", "mount"] }
bitflags = "2.6"
thiserror = "2.0"
byteorder = "1.5"

[dev-dependencies]
tempfile = "3.10"
